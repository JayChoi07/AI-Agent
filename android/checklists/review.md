# review — 표준 준수 검증
읽을 references: [32-code-review, 00-principles, 11-architecture-layers, 12-state-management, 13-navigation, 14-di, 16-domain-layer, 17-compose-ui, 30-testing, 31-ci-cd]
함께 로드할 공식 스킬: [없음]

## 결정 항목
이 체크리스트에는 결정이 없다. 아래 검증 항목 표를 그대로 채운다. 각 행은 통과·해당 없음·위반 중 하나이며, 근거 칸에는 실제 파일·심볼 이름을 적는다. 판정 없이 비워두지 않는다.

| # | 검증 항목 | 규칙 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | 단방향 데이터 흐름 — 상태는 아래로, 이벤트는 위로 | R-00-01 | | |
| 2 | 계층 의존이 ui → domain → data 한 방향, data는 위 계층 참조 없음 | R-11-01 | | |
| 3 | Repository 인터페이스와 구현이 모두 data 계층 | R-11-02 | | |
| 4 | 화면 상태가 `<기능명>UiState` 불변 data class 하나 | R-12-01 | | |
| 5 | 상태 아키텍처 매트릭스 판정이 기록됨 | R-12-02 | | |
| 6 | 목적지 키가 `NavKey` + `@Serializable`, 소유 feature 모듈에 위치 | R-13-01 | | |
| 7 | ViewModel이 `@HiltViewModel` + `@Inject constructor` | R-14-01 | | |
| 8 | UseCase가 public 함수 하나 + `operator fun invoke` | R-16-01 | | |
| 9 | Screen 컴포저블이 상태와 콜백만 받음 | R-17-01 | | |
| 10 | 화면 ViewModel마다 로컬 단위 테스트 존재 | R-30-01 | | |
| 11 | Composable Screen마다 스크린샷 테스트 1개 이상 | R-30-03 | | |

UseCase를 만들지 않은 화면의 8번, feature 모듈이 아닌 변경의 6번처럼 대상이 없는 행은 "해당 없음"으로 적고 이유를 근거 칸에 한 줄 남긴다.

## 구현 순서
게이트는 R-31-01의 순서 그대로 돌린다: `ktlintCheck` → `detektDebug` → `testDebugUnitTest verifyRoborazziDebug` → `assembleDebug`.
넷 중 하나라도 돌리지 못했으면 통과가 아니라 **미실행**으로 적는다. 실행하지 않은 게이트를 통과로 쓰지 않는다.

1. `ktlintCheck` 실행 → 검증: 0 이슈 (R-31-01)
2. `detektDebug` 실행 → 검증: 0 이슈. plain `detekt`는 타입 해석이 없어 `!!`·하드코딩 디스패처를 놓친다 (R-31-01)
3. `testDebugUnitTest` 실행 → 검증: 전부 통과. Konsist 아키텍처 테스트도 이 단계에서 함께 돈다 (R-11-01, R-11-02, R-30-01)
4. `verifyRoborazziDebug` 실행 → 검증: 골든 이미지 비교 통과. 스크린샷 테스트가 있다는 사실이나 단위 테스트 통과는 골든 비교 통과가 아니다. 기준 이미지 재기록(`recordRoborazziDebug`) 없이 통과해야 한다 (R-30-03, R-30-04)
5. `assembleDebug` 실행 → 검증: 빌드 성공 (R-31-01)
6. 위 검증 항목 표 11행을 채운다 → 검증: 빈 칸 없음, 근거에 실제 이름이 있음
7. 어긴 규칙과 사유를 적는다 → 검증: 우회한 규칙을 숨기지 않음, 각 지적에 규칙 ID를 붙임 (R-32-01, R-32-06)

리뷰 코멘트를 쓸 때: 선택적 지적은 `Nit:`을 붙여 머지를 막지 않는다 (R-32-03). 포맷·스타일은 코멘트가 아니라 도구 설정으로 해결한다 (R-32-04). 동등하게 타당한 설계 대안이면 작성자 선택을 존중한다 (R-32-05). 승인 기준은 완벽함이 아니라 코드 건강도 개선이다 (R-32-02).

## 산출물 검증
아래 "표준 준수 보고" 표를 응답 맨 끝에 붙인다. 항목 이름과 순서를 바꾸지 않고, 각 행에 근거가 된 규칙 ID를 적는다 (R-32-01).

```markdown
## 표준 준수 보고
| 항목 | 내용 |
|---|---|
| 요청 유형 | |
| 모듈 위치 | |
| 네비게이션 | |
| 상태 아키텍처 | |
| UseCase | |
| 테스트 | |
| CI | |
| 어긴 규칙 | |
```

제출 전 자기검사 3가지:
- 응답 순서: 유형 → 결정 항목 표 → 구현 → 검증 → 표준 준수 보고
- 인용한 R-ID 전부 제목 확인(오인용 0). 위 "표준 준수 보고" 표의 칸에도 `R-10-04(모듈 간 배치 …)`처럼 `R-ID(제목 요지)` 형태로 적는다 — ID만 적지 않는다
- 게이트 4단계 각각에 통과·미실행 중 하나를 적었는가 (R-31-01)
